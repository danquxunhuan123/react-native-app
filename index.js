/**
 * @format
 */

import {AppRegistry} from 'react-native';
import na from './component/navigation';
import {name as appName} from './app.json';
import GridViewPage from './component/GridViewPage';

AppRegistry.registerComponent(appName, () => GridViewPage);
